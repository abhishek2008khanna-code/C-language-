/*
Q5- Write a program to calculate the sum of the 
numbers occuring in the multiplication table f 8. 
( consider 8 X 1 to 8 X 10).
*/

#include <stdio.h>

int main(){
    int sum = 0;
    for (int i = i; i <=10; i++)
    {
        sum += (8*i);
    }
    printf("The sum of the numbers occuring in the multiplication table of 8 is %d", sum);
    return 0;
}
